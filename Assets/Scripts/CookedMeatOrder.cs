using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CookedMeatOrder : Order
{
    // Start is called before the first frame update
    public CookedMeatOrder()
    {
        orderName = "meat";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
